/**
 * GetAnonymisationGate handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: anonymisation-gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getAnonymisationGate } from "@pilotspan/api-server/domains/anonymisation-gates/handlers";
import type { AnonymisationGatesDDDDependencies } from "@pilotspan/api-server/domains/anonymisation-gates";
import { createAnonymisationGate as createAnonymisationGateEntity } from "@pilotspan/factories/anonymisation-gates";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@pilotspan/tests/shared";
import { ANONYMISATIONGATES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetAnonymisationGate", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AnonymisationGatesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AnonymisationGatesDDDDependencies>(
      ANONYMISATIONGATES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createAnonymisationGateEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetAnonymisationGate, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { gateId: entity.id };

    await getAnonymisationGate(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetAnonymisationGate.execute).toHaveBeenCalledWith(expect.objectContaining({ gateId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { gateId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetAnonymisationGate, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getAnonymisationGate(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
