/**
 * GetReadinessAssessment handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: readiness
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getReadinessAssessment } from "@pilotspan/api-server/domains/readiness/handlers";
import type { ReadinessDDDDependencies } from "@pilotspan/api-server/domains/readiness";
import { createReadinessAssessment as createReadinessAssessmentEntity } from "@pilotspan/factories/readiness";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@pilotspan/tests/shared";
import { READINESS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetReadinessAssessment", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ReadinessDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ReadinessDDDDependencies>(
      READINESS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createReadinessAssessmentEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetReadinessAssessment, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { assessmentId: entity.id };

    await getReadinessAssessment(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetReadinessAssessment.execute).toHaveBeenCalledWith(expect.objectContaining({ assessmentId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { assessmentId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetReadinessAssessment, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getReadinessAssessment(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
