/**
 * UpsertPilotCharter handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: pilots
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { upsertPilotCharter } from "@pilotspan/api-server/domains/pilots/handlers";
import type { PilotsDDDDependencies } from "@pilotspan/api-server/domains/pilots";
import { createCharter as createCharterEntity } from "@pilotspan/factories/pilots";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@pilotspan/tests/shared";
import { PILOTS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpsertPilotCharter", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PilotsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PilotsDDDDependencies>(
      PILOTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createCharterEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { pilotId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpsertPilotCharter, "execute").mockResolvedValue(useCaseResult);

    await upsertPilotCharter(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpsertPilotCharter.execute).toHaveBeenCalledWith(expect.objectContaining({ pilotId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
