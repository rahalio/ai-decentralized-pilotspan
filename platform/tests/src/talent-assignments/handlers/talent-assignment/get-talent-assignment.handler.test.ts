/**
 * GetTalentAssignment handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: talent-assignments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getTalentAssignment } from "@pilotspan/api-server/domains/talent-assignments/handlers";
import type { TalentAssignmentsDDDDependencies } from "@pilotspan/api-server/domains/talent-assignments";
import { createTalentAssignment as createTalentAssignmentEntity } from "@pilotspan/factories/talent-assignments";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@pilotspan/tests/shared";
import { TALENTASSIGNMENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetTalentAssignment", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: TalentAssignmentsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<TalentAssignmentsDDDDependencies>(
      TALENTASSIGNMENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createTalentAssignmentEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetTalentAssignment, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { assignmentId: entity.id };

    await getTalentAssignment(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetTalentAssignment.execute).toHaveBeenCalledWith(expect.objectContaining({ assignmentId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { assignmentId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetTalentAssignment, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getTalentAssignment(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
